pipeline {
    agent {
        dockerfile true
    }

    tools {
        jdk 'jdk8'
       }
       
     stages {
       stage('Example') {
         steps {
           echo 'Hello World'
           }
         }
       }
}
